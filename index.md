1. `$ git status`：列出目前狀態，告知是否有未更新的檔案
2. `$ git log`：查看歷史紀錄        `--oneline`：一行一行印出       `-n 3`：只要印3個紀錄
3. `hashcode`：從`$ git log`裡可以看到所有的代碼
4. `origin`：指向自己存在local的github      `upstream`：遠端指向自己的github